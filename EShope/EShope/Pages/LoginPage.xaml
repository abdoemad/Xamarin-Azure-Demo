<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:viewModels="clr-namespace:EShope.ViewModels"
			 xmlns:resources="clr-namespace:EShope.Resources"
			 x:Class="EShope.Pages.LoginPage">
	<ContentPage.BindingContext>
		<viewModels:LoginViewModel></viewModels:LoginViewModel>
	</ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid HorizontalOptions="Center" VerticalOptions="Center">
			<Grid.RowDefinitions>
				<RowDefinition></RowDefinition>
				<RowDefinition></RowDefinition>
				<RowDefinition></RowDefinition>
			</Grid.RowDefinitions>
			<Label Grid.Row="0" Text="{x:Static resources:AppResources.AppTitle}"></Label>
			<Entry Grid.Row="1" Text="{Binding User.UserName}"></Entry>
			<Button Grid.Row="2" Command="{Binding LoginCommand}"></Button>
        </Grid>
    </ContentPage.Content>
</ContentPage>